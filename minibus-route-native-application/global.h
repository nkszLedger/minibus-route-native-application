#ifndef GLOBAL_H
#define GLOBAL_H

typedef enum {
    REGISTERED_EMPLOYEE,
    REGISTERED_MEMBER
}PersonType;

typedef enum {

    RIGHT_THUMB,
    RIGHT_INDEX,
    RIGHT_MIDDLE,
    RIGHT_RING,
    RIGHT_LITTLE,

    LEFT_THUMB,
    LEFT_INDEX,
    LEFT_MIDDLE,
    LEFT_RING,
    LEFT_LITTLE,

    UNKNOWN_TYPE,

} FingerType;

#endif // GLOBAL_H
